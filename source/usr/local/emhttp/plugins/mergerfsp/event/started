#!/bin/bash

if [ -x /etc/mergerfsp/array_start_complete.sh ]; then
    echo "Executing mergerFS array start complete script (started)..." | logger
    /etc/mergerfsp/array_start_complete.sh 2>&1 | logger
fi
